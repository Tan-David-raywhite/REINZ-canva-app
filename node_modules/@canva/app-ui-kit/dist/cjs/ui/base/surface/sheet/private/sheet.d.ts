import * as React from 'react';
import type { BackButtonEvent } from '../../../mobile_event_handler/mobile_event_handler';
import type { ScrollState } from '../../../scroll/scroll';
import type { SurfaceCloseContext } from '../../internal/close_context';
import type { ContentContainerProps } from '../../internal/content_container';
import type { FooterDivider, HeaderDivider } from '../../internal/surface_dividers';
import type { Trigger } from '../../internal/trigger';
export type { SnapPoint } from '../behavior/behavior';
export type ScreenHeight = 'half' | 'full';
export type AutoHeight = {
    autoUpTo: ScreenHeight;
};
export type OnRequestCloseContext = SurfaceCloseContext<{
    sheet?: {
        action: 'resize' | 'backButton' | 'backdrop';
    };
}>;
export type Height = ScreenHeight | AutoHeight | 'auto';
export type DragHandleVisibility = 'auto' | 'never';
export type SheetProps = {
    ref?: React.Ref<SheetImperativeHandle | null>
    open: boolean
    trigger?: Trigger
    onBackButton?(event: BackButtonEvent): void
    markOutsideInert?: boolean
    onOpenComplete?: () => void
    onCloseComplete?: () => void
} & SharedSheetProps;
export type HeaderAndFooterProps = {
    headerDivider?: HeaderDivider
    header?: React.ReactNode
    footerDivider?: FooterDivider
    footer?: React.ReactNode
};
type EventHandlerProps = {
    onHeightChange?: (height: Height) => void
    onRequestClose?: (context?: OnRequestCloseContext) => void
    onScroll?: (arg: {
        scrollTop: number;
    }) => void
};
type SharedSheetProps = {
    height?: Height | readonly Height[]
    scrollToResizeBelow?: Height
    backdropVisible?: boolean
    handleVisible?: DragHandleVisibility
    handleSafeAreaInsetBottom?: boolean
    _tempVideoOnlyIgnoreVirtualKeyboardOffset?: boolean
    id?: string
    children?: React.ReactNode
    animateResize?: boolean
} & HeaderAndFooterProps & ContentContainerProps & EventHandlerProps;
export declare function Sheet(
 { open, id: idProp, onOpenComplete, onCloseComplete, onBackButton, backdropVisible, markOutsideInert, trigger, ref, ...inner }: SheetProps
): React.ReactNode;
export type SheetImperativeHandle = {
    setHeight: (height: Height) => void
    getScrollState: () => ScrollState
    scrollTo: (options: ScrollToOptions) => void
    scrollBy: (options: ScrollToOptions) => void
    getBoundingClientRect: () => DOMRect
    scrollTop: number
    scrollLeft: number
    scrollHeight: number
    scrollWidth: number
    clientHeight: number
    clientWidth: number
    offsetHeight: number
    offsetWidth: number
    offsetLeft: number
    offsetTop: number
};
type ScrollToOptions = {
    top?: number;
    left?: number;
    behavior?: 'auto' | 'smooth' | 'instant';
} | number;
