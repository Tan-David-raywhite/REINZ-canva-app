import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import 'react';
import { Box } from '../../../../box/box';
import { Button } from '../../../../button/button';
import { CloudUploadIcon } from '../../../../icons/cloud_upload/icon';
import { TrashIcon } from '../../../../icons/trash/icon';
import { Column, Columns } from '../../../../layout/layout';
import { TruncatedText } from '../../../../typography/truncated/truncated';
import { FileInputMessages as Messages } from './file_input.messages';
export const FileInputItem = (props)=>{
    const { label, onDeleteClick, disabled, start, tagName = 'div' } = props;
    const startDecorator = typeof start === 'function' ? start() : start;
    return _jsx(Box, {
        tagName: tagName,
        children: _jsxs(Columns, {
            spacing: "2u",
            alignY: "center",
            children: [
                _jsx(Column, {
                    width: "content",
                    children: startDecorator ?? _jsx(CloudUploadIcon, {
                        size: "medium",
                        tone: disabled ? 'subtlest' : 'content'
                    })
                }),
                _jsx(Column, {
                    children: _jsx(TruncatedText, {
                        tone: disabled ? 'subtlest' : 'content',
                        lineClamp: 1,
                        children: label
                    })
                }),
                _jsx(Column, {
                    width: "content",
                    children: _jsx(Button, {
                        variant: "tertiary",
                        tooltipLabel: Messages.deleteButtonLabel(),
                        icon: TrashIcon,
                        disabled: disabled,
                        onClick: onDeleteClick
                    })
                })
            ]
        })
    });
};
