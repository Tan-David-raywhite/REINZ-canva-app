export type ItemPositions = {
    itemStartEdges: number[]
    itemEndEdges: number[]
    itemCenterPoints: number[]
    itemSizes: number[]
    totalSize: number
};
export type GetItemSize = (index: number) => number;
export declare const getItemPositions: ({ itemCount, getItemSize, gap, paddingBefore, paddingAfter, }: {
    itemCount: number
    getItemSize: GetItemSize
    gap?: number
    paddingBefore?: number
    paddingAfter?: number
}) => ItemPositions;
